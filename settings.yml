use_default_settings: true

server:
  secret_key: "ghost-key"
  bind_address: "0.0.0.0"
  port: 8080
  public_instance: true
  limiter: false
  image_proxy: false

search:
  safe_search: 0
  autocomplete: ""
  formats:
    - html
    - json
